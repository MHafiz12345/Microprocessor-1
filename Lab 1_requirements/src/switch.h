// This prevents the header from being included multiple times
#ifndef SWITCH_H
#define SWITCH_H

// Function to set up switch pin (pin 22) as input
// Also enables pull-up resistor so switch works correctly
void initSwitch();

#endif
